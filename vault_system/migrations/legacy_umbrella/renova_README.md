# Renova vault registry

Renova's active apriori/posteriori data remains at `Renova_te_ipsum/vault_system` because its controller and manager use that path directly. This sub-vault is the canonical registry and future migration destination; do not duplicate or move the live posterior store while Renova is active.
