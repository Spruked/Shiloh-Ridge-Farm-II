# Validator vaults

Each reasoning validator receives a dedicated directory here. The current deductive observation log is mirrored under `deductive/`; inductive and intuitive compatibility vaults are presently empty.
