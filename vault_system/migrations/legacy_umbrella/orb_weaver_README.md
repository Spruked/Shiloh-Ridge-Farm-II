# Orb Weaver vault registry

The live Shiloh Ridge client vault is `/mnt/r/Orb_Weaver_Data_Files/clients/shilohridgekatahdins.com`. It contains current crawl, audit, preflight, website context, recommendations, history, and the local SQLite index.

Do not copy or mutate those files while Orb Weaver is crawling. Import only completed, versioned artifacts into this repository sub-vault, with their crawl ID and source timestamp recorded.
